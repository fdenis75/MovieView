/*import Foundation
import AppKit

struct MovieFile: Identifiable {
    let id = UUID()
    let url: URL
    let relativePath: String
    var thumbnail: NSImage?
    
    var name: String {
        url.lastPathComponent
    }
    
    var aspectRatio: CGFloat {
        thumbnail?.size.width ?? 16.0 / thumbnail?.size.height ?? 9.0
    }
} */
